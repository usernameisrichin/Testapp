import React,{Component} from 'react';
import { ScrollView, View, Text,StyleSheet,Dimensions, TouchableOpacity, ActivityIndicator , SafeAreaView, FlatList} from 'react-native';

var {height, width} = Dimensions.get('window');

class HistoryScreen extends React.Component{

	render(){
		return(
			<SafeAreaView style={{flex: 1}}>
			
			</SafeAreaView>
			)
	}
}